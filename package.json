{
  "name": "egress-tier-pick",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tiny OPNsense-style egress tiers: define, probe, sticky pick, failover",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test src/pick.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
